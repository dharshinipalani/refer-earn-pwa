import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ec7162",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});

export default theme;